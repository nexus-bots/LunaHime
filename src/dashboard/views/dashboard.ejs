<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - LunaHime</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <header class="dashboard-header">
            <div class="logo">
                <img src="/img/logo.png" alt="LunaHime Logo">
                <h1>LunaHime</h1>
            </div>
            <nav>
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/commands">Commands</a></li>
                    <li><a href="/dashboard" class="active">Dashboard</a></li>
                    <li><a href="https://discord.com/api/oauth2/authorize?client_id=<%= client.user.id %>&permissions=8&scope=bot%20applications.commands" target="_blank" class="invite-btn">
                        <i class="fab fa-discord"></i> Add to Discord
                    </a></li>
                    <% if (user) { %>
                        <li class="user-profile">
                            <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="<%= user.username %>">
                            <span><%= user.username %></span>
                            <a href="/auth/logout" class="logout-btn">Logout</a>
                        </li>
                    <% } else { %>
                        <li><a href="/auth/discord" class="login-btn">Login with Discord</a></li>
                    <% } %>
                </ul>
            </nav>
            <div class="mobile-menu-toggle">
                <i class="fas fa-bars"></i>
            </div>
        </header>

        <main class="dashboard-main">
            <div class="dashboard-welcome">
                <h2>Your Servers</h2>
                <p>Select a server to manage LunaHime's music settings.</p>
            </div>

            <div class="server-grid">
                <% if (guilds && guilds.length > 0) { %>
                    <% guilds.forEach(guild => { %>
                        <a href="/dashboard/<%= guild.id %>" class="server-card">
                            <% if (guild.icon) { %>
                                <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" alt="<%= guild.name %>">
                            <% } else { %>
                                <div class="server-icon-placeholder">
                                    <%= guild.name.charAt(0) %>
                                </div>
                            <% } %>
                            <h3><%= guild.name %></h3>
                        </a>
                    <% }); %>

                    <!-- Add Bot to Server Card -->
                    <a href="https://discord.com/api/oauth2/authorize?client_id=<%= client.user.id %>&permissions=8&scope=bot%20applications.commands" target="_blank" class="server-card add-server-card">
                        <div class="add-server-icon">
                            <i class="fas fa-plus"></i>
                        </div>
                        <h3>Add to Server</h3>
                    </a>
                <% } else { %>
                    <div class="no-servers">
                        <i class="fas fa-server"></i>
                        <h3>No Servers Found</h3>
                        <p>You don't have any servers where you have admin permissions and LunaHime is present.</p>
                        <a href="https://discord.com/api/oauth2/authorize?client_id=<%= client.user.id %>&permissions=8&scope=bot%20applications.commands" class="cta-button" target="_blank">
                            <i class="fab fa-discord"></i> Add to Server
                        </a>
                    </div>
                <% } %>
            </div>
        </main>

        <footer class="dashboard-footer">
            <div class="footer-content">
                <div class="footer-logo">
                    <img src="/img/logo.png" alt="LunaHime Logo">
                    <h3>LunaHime</h3>
                    <p>The Ultimate Discord Music Bot</p>
                </div>
                <div class="footer-links">
                    <h3>Quick Links</h3>
                    <ul>
                        <li><a href="/">Home</a></li>
                        <li><a href="/commands">Commands</a></li>
                        <li><a href="/dashboard">Dashboard</a></li>
                        <li><a href="https://discord.com/api/oauth2/authorize?client_id=<%= client.user.id %>&permissions=8&scope=bot%20applications.commands" target="_blank">Add to Discord</a></li>
                    </ul>
                </div>
                <div class="footer-social">
                    <h3>Connect With Us</h3>
                    <div class="social-icons">
                        <a href="#" target="_blank" class="social-icon"><i class="fab fa-discord"></i></a>
                        <a href="#" target="_blank" class="social-icon"><i class="fab fa-github"></i></a>
                        <a href="#" target="_blank" class="social-icon"><i class="fab fa-twitter"></i></a>
                        <a href="#" target="_blank" class="social-icon"><i class="fab fa-instagram"></i></a>
                    </div>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; <%= new Date().getFullYear() %> LunaHime. All rights reserved.</p>
            </div>
        </footer>
    </div>

    <script src="/js/main.js"></script>
</body>
</html>
